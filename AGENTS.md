# 项目协作说明

## 用户背景与沟通方式

- 用户是 Golang 初学者。
- 讲解后端、Go 语法、并发、网络和中间件时，优先从基础概念开始。
- 尽量使用容易理解的语言，避免只给结论或堆砌术语。
- 引入新技术时，要说明它解决什么问题、为什么选择它，以及最小可运行示例。
- 讨论架构时，先解释数据流和组件职责，再讨论性能优化。
- 采用逐步推进的方式；一次聚焦一个重要决策，必要时给出推荐方案和取舍。

## 核心学习重点

- 用户明确希望把“分布式”和“高并发”作为本项目最重要的学习目标，后续技术讲解、方案选择和代码实现都应优先服务于这两个目标。
- 讨论分布式设计时，重点解释网关、房间路由、消息队列、缓存和持久化之间的数据流，以及消息顺序、分区、幂等、故障恢复和水平扩展问题。
- 讨论高并发设计时，重点解释 goroutine、锁、channel、共享状态、背压、慢客户端、限流、热点房间、延迟和吞吐之间的关系，并通过 benchmark、压测、监控或 pprof 验证结论。
- 每个新增功能都要说明它解决了哪个分布式或高并发问题、引入了什么新的瓶颈，以及未来如何扩展；避免为了“功能数量”优先实现与核心目标关系较弱的普通业务功能。
- 当前阶段暂缓 JWT 等身份认证业务，把主要精力放在消息链路、房间分片、Kafka/Redis 演进、重连补偿、背压治理、可观测性和故障演练上；认证功能后续再补充。

## 代码理解门禁

- 只要本项目进入代码创建、修改、重构或修复阶段，就启用 `code-gate` skill。
- 先完成一个小而完整的功能单元，再进行代码理解检查；不要按每几行代码打断开发。
- 一个最小单元应控制在用户可以一次理解的范围内；如果用户表示看不懂，应暂停增加功能，先拆分和解释当前代码。
- 将改动按语义分块，优先讲解核心逻辑、数据流、并发、外部调用和副作用。
- 每个代码单元验证通过后，一次检查一个重要代码块，使用一到两个问题确认用户能预测其行为。
- 用户尚未理解当前重要代码块时，不开始下一个重要功能单元；可以继续解释和修正当前单元。
- 认证、权限、数据库迁移、并发、共享状态、资源释放和破坏性操作必须进行高风险理解检查。
- 不要求逐行背诵；目标是让用户能够用自己的话解释、修改和排查代码。
- 每次启用 Code Gate 时，先简短告知用户；当前代码任务完成且所有检查通过后，再宣布 Gate cleared。

## 每次代码修改前的说明
- 面临重大技术栈抉择之前, 使用grill me skill和用户对话
- 修改代码前，必须先用简短文字建立心智模型，说明本次功能的数据流和涉及的核心概念。
- 必须明确列出将修改的文件，以及每个文件中要修改的内容。
- 必须说明本次新增的外部可观察行为，以及明确暂时不做的内容。
- 修改完成后，说明实际修改了哪些文件、每个文件的实际变化和验证结果。
- 如果工作区中存在与当前任务无关的改动，不得静默覆盖、回滚或提交这些改动。

## 代码注释规范

- 面向 Golang 初学者，关键代码应有足够的中文注释，帮助读者建立数据流和职责模型。
- 注释重点解释为什么这样设计、数据从哪里来、流向哪里、生命周期如何结束，不要逐行翻译代码或重复变量名。
- 所有导出的包、类型、函数和方法都要有符合 Go 文档习惯的注释。
- 涉及 goroutine、mutex、channel、网络连接、资源释放和错误处理时，必须说明共享状态、阻塞行为、退出条件和失败场景。
- 注释应保持适度：简单的赋值、循环和语法不需要逐行注释；复杂业务分支和初学者无法从代码直接推断的原因必须注释。
- 修改代码行为时必须同步更新过时注释，不能让注释描述与实际代码不一致。

## 代码与项目目标

- 项目名称：DanmuFlow
- 技术方向：以 Go 为主的高并发直播间弹幕系统。
- 目标 GitHub 仓库：`git@github.com:1012-Penn/DanmuFlow.git`
- 后续提交或推送前，先检查工作区状态、确认改动范围，并向用户说明将要提交的内容。
- 未经用户明确要求，不执行推送、发布或其他外部写操作。
- 如果 GitHub CLI 认证失效，由 Codex 启动 `gh auth login` 设备认证流程，并把 GitHub 提供的一次性验证码和网址交给用户，由用户完成浏览器认证。

## 当前设计记录

- 架构决策记录位于 `.project-notes/architecture-decisions.md`。
- `.project-notes/` 是本地设计资料，已加入 `.gitignore`，不提交到 GitHub。
